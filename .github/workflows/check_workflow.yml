name: Typescript Check PR

on:
  pull_request: 
    branches:
      - "dev"

jobs: 
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Install 
        run: npm install 

      - name: Check
        run: npm run check

